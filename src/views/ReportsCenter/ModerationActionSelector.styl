.ModerationActionSelector {
    .action-buttons {
        display: flex;
        justify-content: flex-end;
    }

    .voter-note-container {
        display: flex;
        align-items: flex-start;
        gap: 0.5rem;

        textarea {
            flex: 1;
        }
    }

    .character-count {
        font-size: 0.75rem;
        opacity: 0.6;
        padding-top: 0.25rem;
    }
}